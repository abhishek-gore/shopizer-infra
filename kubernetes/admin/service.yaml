apiVersion: v1
kind: Service
metadata:
  name: shopizer-admin
  namespace: shopizer-local
spec:
  selector:
    app: shopizer-admin
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
